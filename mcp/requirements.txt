# cardputer-mcp host-side dependencies.
#
# Pinned to lower bounds known to work. Update as needed; the MCP SDK
# API has shifted across versions so re-test the stdio entry point
# after a bump.

mcp>=1.0.0
# bleak is not yet imported in iter 1 (transport is stubbed) but
# pre-declared so `pip install -r` produces a complete environment
# ready for iter 2.
bleak>=0.21.0

# HTTP transport (iter 5: streamable-http daemon behind an MCP tunnel).
# starlette + uvicorn ship transitively with the mcp SDK, but the
# tunnel/daemon path imports them directly, so pin them explicitly.
starlette>=1.0.0
uvicorn>=0.30.0
